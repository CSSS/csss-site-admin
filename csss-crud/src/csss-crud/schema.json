{
  "$id": "table",
  "title": "Creates a new database table, dialog, and sources",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component."
    }
  },
  "required": ["name"]
}
