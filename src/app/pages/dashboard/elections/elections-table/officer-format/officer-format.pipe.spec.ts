import { OfficerFormatPipe } from './officer-format.pipe';

describe('OfficerFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new OfficerFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
