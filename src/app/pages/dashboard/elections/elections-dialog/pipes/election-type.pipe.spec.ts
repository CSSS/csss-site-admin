import { ElectionTypePipe } from './election-type.pipe';

describe('ElectionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ElectionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
